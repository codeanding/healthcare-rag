// Sonnet 4.6 Bedrock list pricing (USD per token), as of 2026.
// Used by estimateCostUsd to render per-message cost estimates in the UI.
export const COST_INPUT_PER_TOKEN = 3 / 1_000_000;
export const COST_OUTPUT_PER_TOKEN = 15 / 1_000_000;
